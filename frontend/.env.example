# Optional. Where the Vite dev server proxies "/api" requests during
# development. Defaults to the backend's default port (see backend/app/config.py).
# Only change this if you run the FastAPI backend on a different host/port.
VITE_API_TARGET=http://localhost:58734
