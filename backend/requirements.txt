# =====================================================================
# FULL requirements: ingest server / development machine.
# Includes torch via sentence-transformers (embedding model host).
# Worker PCs use requirements-worker.txt instead (no torch, ~2 GB lighter).
# Pinned for reproducible builds; bump deliberately and re-run tests.
# =====================================================================

# --- Web / API ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
sse-starlette==3.4.5
pydantic-settings==2.14.2
python-dotenv==1.2.2
httpx==0.28.1

# --- RAG core ---
llama-index-core==0.14.23
llama-index-retrievers-bm25==0.7.1
PyStemmer==2.2.0.3                     # <3 required by llama-index-retrievers-bm25
llama-index-readers-file==0.6.0        # per-page PDF parsing (page_label metadata)

# --- Embedding model host (ingest server only) ---
llama-index-embeddings-huggingface==0.7.0   # Qwen3-Embedding-0.6B

# --- Generation LLM providers ---
llama-index-llms-google-genai==0.9.6   # Gemini (default)
# llama-index-llms-ollama==0.10.1      # optional on-prem alternative
# llama-index-llms-openai              # optional
# llama-index-llms-anthropic           # optional
